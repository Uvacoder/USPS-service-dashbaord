import * as d3 from "d3";

const compositeGraphDims = {
  grapHeight: 300,
  graphWidth: 300,
  barWidth: 5,
};

// export const yScale = d3.scaleLinear().domain([0, 100]).range([300, 0]);

// export default yScale;
